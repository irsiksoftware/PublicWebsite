## Summary
Implements sticky header with glassmorphism effect per issue #253

## Changes
- Removed inline `position: relative` style from header element
- Header already styled with sticky positioning, glassmorphism, and `.scrolled` class handling in `css/styles.css`
- JavaScript scroll handler already active in `js/sticky-header.js`

## Test Plan
- [x] Header stays visible when scrolling
- [x] Glassmorphism effect (blur + semi-transparent bg) renders correctly
- [x] `.scrolled` class adds box-shadow when page scrolls
- [x] Smooth transitions work (0.3s ease)

Closes #253